{
  "name": "@social-hustle/utils-fetch",
  "version": "2.0.0",
  "description": "A drop-in replacement (with some extras) for Axios that uses the Fetch API.",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsup",
    "lint": "eslint",
    "test": "vitest run --passWithNoTests",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "tsc": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "isomorphic-fetch": "3.0.0",
    "msw": "1.3.1"
  },
  "publishConfig": {
    "access": "public"
  }
}
